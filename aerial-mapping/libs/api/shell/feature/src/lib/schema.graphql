type User {
  userID: Int!
  user_name: String
  user_surname: String
  user_email: String!
}

type Query {
  user(name: String): User
  pingAuthentication: String!
}

type Mutation {
  login(email: String, password: String): User
}
